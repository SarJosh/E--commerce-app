import { View } from "react-native";

export default function Loader() {
  return (
    <View
      style={{
        height: 200,
        backgroundColor: "#e0e0e0",
        margin: 8,
        borderRadius: 10,
      }}
    />
  );
}