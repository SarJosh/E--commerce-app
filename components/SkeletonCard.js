import { View } from "react-native";

export default function SkeletonCard() {
  return (
    <View
      style={{
        backgroundColor: "#e0e0e0",
        height: 200,
        margin: 8,
        borderRadius: 10,
      }}
    />
  );
}